'use client'
export default function ImgAlvoSorteio(){
    const imagens = ['/prato_rotacao.png', '/babul.png'];
    const imagem = math.floor(math.random() * imagens.lenght);
    return(
        {imagem}    
        
    )
}